##########################################
#  ____            _     _ __  __  ____  #
# / ___| _   _ ___| |__ (_)  \/  |/ ___| #
# \___ \| | | / __| '_ \| | |\/| | |     #
#  ___) | |_| \__ \ | | | | |  | | |___  #
# |____/ \__,_|___/_| |_|_|_|  |_|\____| #
##########################################
# OpenAI-compatible API endpoint (works with Ollama, vLLM, Unsloth, OpenAI, etc.)
# Ollama example: http://localhost:11434/v1/chat/completions
# OpenAI example: https://api.openai.com/v1/chat/completions
endpoint: "http://localhost:11434/v1/chat/completions"

# API key — use "ollama" or any placeholder for local servers that ignore auth
api-key: "ollama"

# Model name as understood by your provider
model: "gemma3n:e2b"

# Optional system prompt prepended to every new chat
system-prompt: "You are a helpful assistant inside a Minecraft server. Keep replies concise and game-friendly. Use plain text paragraphs only—no markdown dividers, underscores, or special tokens."

# Stop sequences sent to the API (prevents Gemma/Ollama template tokens leaking into replies)
stop-sequences:
  - "<end_of_turn>"
  - "<start_of_turn>"

# Optional sampling temperature (-1 = omit, let the provider default). Gemma 3n works well at 1.0.
temperature: 1.0

# HTTP request timeout in seconds
timeout-seconds: 120

# Max prior messages kept in context (user + assistant pairs)
max-history-messages: 20
